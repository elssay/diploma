from app import db
from app.models import Auto


# создаем экземпляр класса User
#new_auto = Auto(title="Lada",price = 100, description = 'f', transmission = True )

# добавляем изменения в базу данных (при этом база не сохраняется)
# db.session.add(Auto(title="Lada", price = 100, description = 'fgd'))
# db.session.add(Auto(title="Volga", price = 200, description = 'kjh', transmission = True))


# сохраняем изменения в базе
#db.session.commit()
